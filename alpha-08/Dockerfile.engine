FROM python:3.9-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
# The calibration engine starts the FastAPI server internally
CMD ["python", "engine/calibration_engine.py"]
