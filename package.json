{
  "name": "dashboard",
  "version": "1.0.0",
  "description": "Static dashboard deployment",
  "scripts": {
    "start": "node server.js",
    "build": "./build.sh",
    "postinstall": "npm install express --save"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
