{
  "name": "alpha-orion",
  "version": "1.0.0",
  "description": "Alpha Orion - AI-Powered DeFi Trading Platform",
  "scripts": {
    "build": "echo 'No build needed - services are deployed via render.yaml'",
    "start": "echo 'No start needed - services are deployed via render.yaml'"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
